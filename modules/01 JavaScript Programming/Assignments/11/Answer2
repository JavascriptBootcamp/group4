var index = 1;

while (index<=100) {
    if (!(index%15)) {
        console.log("fizzbuzz");
    }
    else if(index%3 && !(index%5)) {
        console.log("buzz");
    }else if (index%5 && !(index%3)){
        console.log("fizz");
    } else
        console.log(index);
    index++;
}
