var arr = [1, 2, 5, 7];
var otherArr = [];

var filtered = myFilter(arr, function(item){
     return item % 2 === 0;
});

console.log(filtered);

for (var index = 0; index < array.length; index++) {
    otherArr.push(myFilter(arr[index]));
}
