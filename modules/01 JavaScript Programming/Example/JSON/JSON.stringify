כאשר שולחים נתונים לשרת האינטרנט שלנו הנתונים צריכים להיות סטרינג/מחרוזת
המתודה הזאת ממירה אובייקט לסטרינג/מחרוזת כדי לשלוח את הנתונים לשרת האינטרנט

var obj = { x: 5 };
var myJSON = JSON.stringify(obj2);
console.log(myJSON);//'{"x":5}'
