import { TemplateDrivenForm } from './template-driven-form';

describe('TemplateDrivenForm', () => {
  it('should create an instance', () => {
    expect(new TemplateDrivenForm()).toBeTruthy();
  });
});
