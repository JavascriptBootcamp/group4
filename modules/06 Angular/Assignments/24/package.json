{
  "name": "05",
  "version": "1.0.0",
  "description": "<p>create new folder.</p>\r <p>initialize <code>package.json</code> using <code>npm init</code>.<p>\r <p>install Express using <code>npm install express</code>\r <p>create a <code>main.js</code> file.</p>\r <p>create an empty array of chat messages.</p>\r <p>initialize an Express application.</p>\r <p>support the following CRUD (Create, Read, Update, Delete) operations:</p>\r <ol>\r     <li>POST (Create): Create a new message. each chat message should contain the following fields: \"ID\" (random number), \"Author\", \"Message\" fields (all fields should be transferred as part of the request BODY)</li>\r     <li>GET (Read): Read all chat messages</li>\r     <li>PUT (Update): Update \"Message\" field by \"ID\" (\"ID\" should be transferred as querystring, message should be part of the request BODY)</li>\r     <li>DELETE (Delete): Delete \"Message\" field by \"ID\" (\"ID\" should be transferred as querystring)</li>\r </ol>\r <h3>Bonus:</h3>\r <p>build an Angular application which displays all chat messages and a form with two fields: \"Your Name\" and \"Message\".<p>\r <p>submitting the form will initiate a POST request to the server and will add a new message.</p>\r <p>allow the rest of the operations as well: Updating a message (by clicking an \"Edit\" icon and modifying the message using a form) and Deleting a message (by clicking a \"Delete\" icon).</p>",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.3"
  }
}
