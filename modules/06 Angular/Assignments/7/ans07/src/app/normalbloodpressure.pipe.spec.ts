import { NormalbloodpressurePipe } from './normalbloodpressure.pipe';

describe('NormalbloodpressurePipe', () => {
  it('create an instance', () => {
    const pipe = new NormalbloodpressurePipe();
    expect(pipe).toBeTruthy();
  });
});
