import { AnagramPipePipe } from './anagram-pipe.pipe';

describe('AnagramPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AnagramPipePipe();
    expect(pipe).toBeTruthy();
  });
});
