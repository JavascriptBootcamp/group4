{
  "name": "06",
  "version": "1.0.0",
  "description": "<h2>Server (Node.js)</h2>\r <p>create a folder named <code>documents</code> with 5 different files. for example: file1.json, file2.json, file3.json, file4.json, file4.json and file5.json.</p>\r <p>create an Express application.</p>\r <p>create a POST handler to the path <code>/file</code> which accepts a list of file names as part of the request BODY.</p>\r <p>the POST handler should look for the files and create a new TXT file <code>found_files.txt</code> with the list of found files - one file name per line (using Node.js streams and pipe).</p>\r <p>the POST response should be \"OK\" (status code 200) on a success operation or \"Error\" (status code 500) on a failure.</p>\r <p>add a middleware which logs the list of file names using <code>console.log</code>.</p>\r <h2>Example</h2>\r <h3>Request with the files file1.json, file2.json and file99.json:</h3>\r <code>\r (POST) http://localhost:5000\r (BODY) {\r     files: [\"file1.json\", \"file2.json\", \"file99.json\"]\r }\r </code>\r <h3>Should create <code>found_files.txt</code> with the list of found files</h5>\r <code>\r file1.json\r file2.json\r </code>\r <h3>And log</h5>\r <code>[\"file1.json\", \"file2.json\", \"file99.json\"]</code>\r <h2>Client (Angular)</h2>\r <p>build an Angular application which implement a search form with one input field for file name to search.<p>\r <p>submitting the form will initiate a POST request to the server and will display the matching file names.</p>",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
