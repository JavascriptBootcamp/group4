import { SrcYoutubePipe } from './src-youtube.pipe';

describe('SrcYoutubePipe', () => {
  it('create an instance', () => {
    const pipe = new SrcYoutubePipe();
    expect(pipe).toBeTruthy();
  });
});
