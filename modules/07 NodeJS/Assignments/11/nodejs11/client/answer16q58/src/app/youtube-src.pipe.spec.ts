import { YoutubeSrcPipe } from './youtube-src.pipe';

describe('YoutubeSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new YoutubeSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
