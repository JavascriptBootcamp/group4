import { SmallImgDirective } from './small-img.directive';

describe('SmallImgDirective', () => {
  it('should create an instance', () => {
    const directive = new SmallImgDirective();
    expect(directive).toBeTruthy();
  });
});
