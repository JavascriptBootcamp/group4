import { MonthToColorDirective } from './month-to-color.directive';

describe('MonthToColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MonthToColorDirective();
    expect(directive).toBeTruthy();
  });
});
