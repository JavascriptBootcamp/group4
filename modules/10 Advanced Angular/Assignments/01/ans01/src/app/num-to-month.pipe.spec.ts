import { NumToMonthPipe } from './num-to-month.pipe';

describe('NumToMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new NumToMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
