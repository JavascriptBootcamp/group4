import { ColormonthDirective } from './colormonth.directive';

describe('ColormonthDirective', () => {
  it('should create an instance', () => {
    const directive = new ColormonthDirective();
    expect(directive).toBeTruthy();
  });
});
