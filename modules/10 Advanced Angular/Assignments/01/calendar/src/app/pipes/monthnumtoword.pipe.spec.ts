import { MonthnumtowordPipe } from './monthnumtoword.pipe';

describe('MonthnumtowordPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthnumtowordPipe();
    expect(pipe).toBeTruthy();
  });
});
