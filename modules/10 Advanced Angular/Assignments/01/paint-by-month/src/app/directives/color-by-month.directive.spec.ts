import { ColorByMonthDirective } from './color-by-month.directive';

describe('ColorByMonthDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorByMonthDirective();
    expect(directive).toBeTruthy();
  });
});
