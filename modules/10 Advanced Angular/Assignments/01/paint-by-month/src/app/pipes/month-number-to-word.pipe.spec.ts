import { MonthNumberToWordPipe } from './month-number-to-word.pipe';

describe('MonthNumberToWordPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthNumberToWordPipe();
    expect(pipe).toBeTruthy();
  });
});
