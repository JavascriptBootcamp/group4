import { MaskCreditCardDirective } from './mask-credit-card.directive';

describe('MaskCreditCardDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskCreditCardDirective();
    expect(directive).toBeTruthy();
  });
});
