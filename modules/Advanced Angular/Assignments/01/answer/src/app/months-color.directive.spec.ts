import { MonthsColorDirective } from './months-color.directive';

describe('MonthsColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MonthsColorDirective();
    expect(directive).toBeTruthy();
  });
});
