import { PaintByMonthDirective } from './paint-by-month.directive';

describe('PaintByMonthDirective', () => {
  it('should create an instance', () => {
    const directive = new PaintByMonthDirective();
    expect(directive).toBeTruthy();
  });
});
