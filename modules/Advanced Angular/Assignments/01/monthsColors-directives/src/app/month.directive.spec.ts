import { MonthDirective } from './month.directive';

describe('MonthDirective', () => {
  it('should create an instance', () => {
    const directive = new MonthDirective();
    expect(directive).toBeTruthy();
  });
});
